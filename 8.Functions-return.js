/* 
Functions / Funções 

Funcão void (vazia), não retorna nada.
Função return, retorna um valor para quem a chamou.
*/

function sub (value, value2) {
    const result = value + value2
    return result
}

const result = sum(10, 20)
console.log(result + " Resultado")